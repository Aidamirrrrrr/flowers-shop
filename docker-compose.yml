services:
  db:
    image: postgres:16-alpine
    ports:
      - '5433:5432'
    environment:
      POSTGRES_USER: flowers
      POSTGRES_PASSWORD: flowers
      POSTGRES_DB: flowers_shop
    volumes:
      - flowers_pg_data:/var/lib/postgresql/data

volumes:
  flowers_pg_data:
